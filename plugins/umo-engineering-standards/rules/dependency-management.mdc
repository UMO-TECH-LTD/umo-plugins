---
description: Dependency management — exact versions, scoped upgrades, and verification after changes.
alwaysApply: true
---

# Dependency Management

Use exact versions in dependency manifests. Avoid broad upgrades unless explicitly
requested. After changing dependencies, load the `dependency-management` skill and
verify the affected package or repository.
